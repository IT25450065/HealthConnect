PORT=5000
JWT_SECRET=replace-with-a-long-random-secret

# MySQL is the application default. Use file only to temporarily run the legacy JSON store.
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=healthconnect_pharmacy
